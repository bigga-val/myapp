<!DOCTYPE html>

<title>CommandeApprobateur</title>

{% block body %}
    <h1>CommandeApprobateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_approbateur.id }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ commande_approbateur.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_approbateur_index') }}">back to list</a>

    <a href="{{ path('app_commande_approbateur_edit', {'id': commande_approbateur.id}) }}">edit</a>

    {{ include('commande_approbateur/_delete_form.html.twig') }}
{% endblock %}
