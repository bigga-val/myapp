<form method="post" action="{{ path('app_commande_reception_delete', {'id': commande_reception.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_reception.id) }}">
    <button class="btn">Delete</button>
</form>
