<!DOCTYPE html>

<title>CommandeReception</title>

{% block body %}
    <h1>CommandeReception</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_reception.id }}</td>
            </tr>
            <tr>
                <th>ReceptionDate</th>
                <td>{{ commande_reception.ReceptionDate ? commande_reception.ReceptionDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>QuantiteRecue</th>
                <td>{{ commande_reception.QuantiteRecue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_reception_index') }}">back to list</a>

    <a href="{{ path('app_commande_reception_edit', {'id': commande_reception.id}) }}">edit</a>

    {{ include('commande_reception/_delete_form.html.twig') }}
{% endblock %}
