{% extends 'components/layout/default.html.twig' %}
{% block content %}

<div class="panel">
    <div class="flex justify-between mb-3">
        <h1>Ajouter un livre</h1>
        <a class="btn btn-secondary" href="{{ path('app_livre_index') }}">Voir la liste</a>
    </div>
</div>

<div class="panel">
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            {{ include('livre/_form.html.twig') }}
        </div>
        <div class="col-md-1"></div>
    </div>
</div>

{% endblock %}
