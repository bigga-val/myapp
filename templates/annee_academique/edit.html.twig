{% extends 'components/layout/default.html.twig' %}
{% block content %}

<div class="panel">
    <div class="flex justify-between mb-3">
        <h1>Modifier l'année académique — {{ annee.libelle }}</h1>
        <a class="btn btn-primary" href="{{ path('app_annee_academique_index') }}">Voir la liste</a>
    </div>
</div>

<div class="panel">
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ include('annee_academique/_form.html.twig') }}
        </div>
        <div class="col-md-2"></div>
    </div>
</div>

{% endblock %}
